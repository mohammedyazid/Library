class meth():
    window=[]
    def push_signup(window):
        #calling the register window class to append the new window without closing the login window
        meth.window.append(window)
        window.show()